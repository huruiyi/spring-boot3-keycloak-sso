# 单点登出测试指南

## 🔍 问题现象

App A 退出登录后，App B 刷新页面仍然显示已登录状态。

## 📋 诊断步骤

### 步骤 1: 运行诊断脚本

```bash
诊断单点登出.bat
```

**预期结果**：
- 两个 "Logout successful" 响应
- 两个 HTTP 200 状态码

### 步骤 2: 检查 Backchannel logout 请求

1. **重启应用**：
   ```bash
   restart-all.bat
   ```

2. **清除浏览器 Cookie**

3. **登录测试**：
   - 访问 App A 并登录
   - 访问 App B（应该自动登录）

4. **单点登出测试**：
   - 在 App A 点击"退出登录"
   - **立即查看 App B 控制台**
   - 应该看到：
     ```
     ========================================
     【App B】收到 Backchannel logout 请求
     时间: ...
     用户ID: ...
     会话ID: ...
     总共使 X 个会话失效
     ========================================
     ```

### 步骤 3: 检查会话状态

在浏览器中访问：
- App A 会话状态：`http://localhost:10000/api/session/status`
- App B 会话状态：`http://localhost:20000/api/session/status`

**登录前**：
```json
{
  "authenticated": false,
  "sessionId": "...",
  "sessionCreationTime": "...",
  "sessionLastAccessTime": "..."
}
```

**登录后**：
```json
{
  "authenticated": true,
  "sessionId": "...",
  "sessionCreationTime": "...",
  "sessionLastAccessTime": "...",
  "username": "huruiyi.fairy.vip",
  "userId": "2b83cc1b-87ef-4615-b2cd-e823c33ccfb9"
}
```

**App A 退出后，App B 的状态应该变为**：
```json
{
  "authenticated": false,
  "sessionId": "...",  // 可能是新的会话ID
  "sessionCreationTime": "...",
  "sessionLastAccessTime": "..."
}
```

## 🚨 可能的问题

### 问题 1: App B 没有收到 Backchannel logout 请求

**症状**：App B 控制台没有显示 Backchannel logout 日志

**原因**：
1. Keycloak 中 app-b 客户端的 Backchannel logout URL 配置错误
2. Keycloak 无法访问 App B 的端点
3. `Backchannel logout session required` 没有开启

**解决方案**：
1. 检查 Keycloak 中 app-b 客户端配置：
   - Backchannel logout URL: `http://localhost:20000/logout/connect/back-channel/keycloak`
   - Backchannel logout session required: `ON`

### 问题 2: 收到请求但会话没有失效

**症状**：看到 Backchannel logout 日志，但 `/api/session/status` 仍显示 `authenticated: true`

**原因**：
1. SessionRegistry 没有正确跟踪会话
2. 会话失效方法不正确
3. 浏览器缓存问题

**解决方案**：
1. 强制刷新页面（Ctrl + F5）
2. 检查 SessionRegistry 配置
3. 查看详细的 Backchannel logout 日志

### 问题 3: Keycloak 配置问题

**症状**：App A 和 App B 都没有收到 Backchannel logout 请求

**原因**：
1. 客户端配置错误
2. Backchannel logout 功能没有启用
3. 网络连接问题

**解决方案**：
1. 重新检查 Keycloak 客户端配置
2. 查看 Keycloak 事件日志
3. 测试网络连通性

## 🔧 调试技巧

### 1. 实时监控会话状态

在两个浏览器标签页中分别打开：
- `http://localhost:10000/api/session/status`
- `http://localhost:20000/api/session/status`

在单点登出测试时，实时刷新这两个页面查看状态变化。

### 2. 查看 Keycloak 事件日志

1. 登录 Keycloak Admin Console: `http://localhost:8080/admin`
2. 进入 `Realm Settings` → `Events`
3. 启用 `Save Events`
4. 查看 LOGOUT 事件的详细信息

### 3. 网络抓包

使用浏览器开发者工具的网络标签，查看：
1. 登出请求是否发送到 Keycloak
2. Keycloak 是否返回正确的响应
3. 是否有 Backchannel logout 请求发送到应用

### 4. 手动测试 Backchannel logout

```bash
# 测试 App B 端点
curl -v -X POST http://localhost:20000/logout/connect/back-channel/keycloak \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "logout_token=test_token"
```

查看 App B 控制台是否显示日志。

## 🎯 完整测试流程

### 1. 准备工作

```bash
# 重启应用
restart-all.bat

# 运行诊断
诊断单点登出.bat
```

### 2. 登录测试

1. 清除浏览器所有 Cookie
2. 访问 `http://localhost:10000/` 并登录
3. 检查会话状态：`http://localhost:10000/api/session/status`
4. 访问 `http://localhost:20000/`（应该自动登录）
5. 检查会话状态：`http://localhost:20000/api/session/status`

### 3. 单点登出测试

1. 在 App A 点击"退出登录"
2. **立即查看 App B 控制台**
3. 检查 App B 会话状态：`http://localhost:20000/api/session/status`
4. 刷新 App B 主页：`http://localhost:20000/home`

### 4. 预期结果

- App B 控制台显示 Backchannel logout 日志
- App B 会话状态变为 `authenticated: false`
- 刷新 App B 主页时被重定向到登录页面

## 📞 如果仍然不工作

请提供以下信息：

1. **App B 控制台输出**（在 App A 退出时）
2. **App B 会话状态**：
   - 退出前：`http://localhost:20000/api/session/status`
   - 退出后：`http://localhost:20000/api/session/status`
3. **Keycloak 客户端配置截图**（app-b 的 Backchannel logout 设置）
4. **诊断脚本输出**

这样我们就能精确定位问题所在！
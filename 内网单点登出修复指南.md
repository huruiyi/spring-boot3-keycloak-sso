# 内网单点登出修复指南

## 当前环境

- ✅ Keycloak: `http://localhost:8080`
- ✅ App A: `client-id: app-a`, 端口 10000
- ✅ App B: `client-id: app-b`, 端口 20000
- ✅ 内网环境，Keycloak 可以访问应用
- ❌ 单点登出不工作

## 立即诊断步骤

### 步骤 1: 测试 Backchannel logout 端点

运行测试脚本：
```bash
test-backchannel-logout.bat
```

**预期结果**：
```
Logout successful
Logout successful
```

如果失败，说明端点配置有问题。

### 步骤 2: 检查 Keycloak 配置

在 Keycloak Admin Console (`http://localhost:8080/admin`) 中：

#### 检查 app-a 客户端

1. 进入 `Clients` → `app-a`
2. 点击 `Settings` 标签
3. 确认以下配置：

**Valid redirect URIs**:
```
http://localhost:10000/login/oauth2/code/keycloak
http://appa.tbk.com/login/oauth2/code/keycloak
```

**Valid post logout redirect URIs**:
```
http://localhost:10000
http://localhost:10000/
http://appa.tbk.com
http://appa.tbk.com/
```

4. 点击 `Advanced` 标签
5. 滚动到 **Logout settings** 部分
6. 配置：

**Backchannel logout URL**:
```
http://localhost:10000/logout/connect/back-channel/keycloak
```

**Backchannel logout session required**: `ON` ✅

**Backchannel logout revoke offline sessions**: `ON` ✅

7. 点击 `Save`

#### 检查 app-b 客户端

1. 进入 `Clients` → `app-b`
2. 重复上述步骤，但使用：

**Valid redirect URIs**:
```
http://localhost:20000/login/oauth2/code/keycloak
http://appb.tbk.com/login/oauth2/code/keycloak
```

**Valid post logout redirect URIs**:
```
http://appb.tbk.com
http://appb.tbk.com/
http://localhost:20000
http://localhost:20000/
```

**Backchannel logout URL**:
```
http://localhost:20000/logout/connect/back-channel/keycloak
```

### 步骤 3: 测试单点登出

1. **重启应用**
   ```bash
   restart-all.bat
   ```

2. **清除浏览器数据**
   - 按 `Ctrl + Shift + Delete`
   - 清除所有 Cookie

3. **登录测试**
   - 访问 `http://localhost:10000/` 或 `http://appa.tbk.com/`
   - 登录
   - 访问 `http://localhost:20000/` 或 `http://appb.tbk.com/`
   - 应该自动登录 ✅

4. **单点登出测试**
   - 在 App A 点击"退出登录"
   - **立即查看 App B 的控制台输出**
   - 应该看到：
     ```
     ========================================
     【App B】收到 Backchannel logout 请求
     时间: ...
     会话已成功失效
     ========================================
     ```
   - 刷新 App B 页面
   - 应该显示登录页面 ✅

## 常见问题和解决方案

### 问题 1: 没有收到 Backchannel logout 请求

**症状**: App A 退出后，App B 控制台没有任何输出

**可能原因**:
1. Keycloak 中没有配置 Backchannel logout URL
2. Backchannel logout URL 配置错误
3. `Backchannel logout session required` 没有开启

**解决方案**:
1. 按照上述步骤检查 Keycloak 配置
2. 确保 URL 使用 `http://localhost:端口` 格式
3. 确保 `Backchannel logout session required` 为 `ON`

### 问题 2: 收到请求但会话没有失效

**症状**: 看到了 Backchannel logout 请求日志，但刷新页面仍然显示已登录

**可能原因**:
1. 会话失效代码有问题
2. 浏览器缓存了页面
3. Cookie 没有被正确清除

**解决方案**:

让我改进 BackchannelLogoutController：

```java
@PostMapping("/logout/connect/back-channel/keycloak")
public ResponseEntity<String> backchannelLogout(
        @RequestParam(value = "logout_token", required = false) String logoutToken,
        @RequestBody(required = false) String body,
        HttpServletRequest request) {
    
    System.out.println("========================================");
    System.out.println("【App X】收到 Backchannel logout 请求");
    System.out.println("时间: " + LocalDateTime.now());
    System.out.println("来源 IP: " + request.getRemoteAddr());
    System.out.println("Logout token: " + (logoutToken != null ? "存在" : "不存在"));
    
    // 获取所有会话并使其失效
    HttpSession session = request.getSession(false);
    if (session != null) {
        System.out.println("使会话失效: " + session.getId());
        try {
            session.invalidate();
            System.out.println("会话已成功失效");
        } catch (Exception e) {
            System.out.println("会话失效失败: " + e.getMessage());
        }
    } else {
        System.out.println("没有找到活动会话");
    }
    
    System.out.println("========================================");
    
    return ResponseEntity.ok("Logout successful");
}
```

### 问题 3: Keycloak 无法访问应用

**症状**: Keycloak 日志显示连接失败

**解决方案**:
1. 确保应用正在运行
2. 确保防火墙没有阻止连接
3. 使用 `localhost` 而不是域名

### 问题 4: CSRF 保护问题

**症状**: Backchannel logout 请求返回 403

**解决方案**: 已在 SecurityConfig 中配置 CSRF 例外

## 高级调试

### 启用 Keycloak 事件日志

1. 登录 Keycloak Admin Console
2. 进入 `Realm Settings` → `Events` 标签
3. 启用 `Save Events`
4. 在 `Event Types` 中选择 `LOGOUT`
5. 点击 `Save`

### 查看详细日志

在测试单点登出时，查看：
1. App A 和 App B 的控制台输出
2. Keycloak 的 `Events` 页面
3. 浏览器的开发者工具网络标签

### 手动测试 Backchannel logout

```bash
# 测试 App A
curl -v -X POST http://localhost:10000/logout/connect/back-channel/keycloak \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "logout_token=eyJ0eXAiOiJKV1QiLCJhbGciOiJSUzI1NiJ9.test"

# 测试 App B  
curl -v -X POST http://localhost:20000/logout/connect/back-channel/keycloak \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "logout_token=eyJ0eXAiOiJKV1QiLCJhbGciOiJSUzI1NiJ9.test"
```

## 完整的工作流程

当单点登出正常工作时：

```
用户在 App A 点击退出
    ↓
App A 调用 /logout
    ↓
Spring Security 重定向到 Keycloak logout endpoint
    ↓
Keycloak 清除全局会话
    ↓
Keycloak 查找该用户的所有活动客户端
    ↓
Keycloak 发送 POST 请求到 App A
    POST http://localhost:10000/logout/connect/back-channel/keycloak
    ↓
Keycloak 发送 POST 请求到 App B
    POST http://localhost:20000/logout/connect/back-channel/keycloak
    ↓
App A 和 App B 的 BackchannelLogoutController 处理请求
    ↓
使各自的会话失效
    ↓
Keycloak 重定向用户到 App A 首页
    ↓
用户访问任何应用都需要重新登录
```

## 检查清单

在测试之前，确认：

### Keycloak 配置
- [ ] app-a 客户端已创建
- [ ] app-b 客户端已创建
- [ ] 两个客户端都配置了正确的 redirect URIs
- [ ] 两个客户端都配置了 Backchannel logout URL
- [ ] `Backchannel logout session required` 都是 `ON`

### 应用配置
- [ ] App A 使用 `client-id: app-a`
- [ ] App B 使用 `client-id: app-b`
- [ ] 客户端密钥正确
- [ ] issuer-uri 指向正确的 Keycloak

### 网络连通性
- [ ] Keycloak 可以访问 `http://localhost:10000`
- [ ] Keycloak 可以访问 `http://localhost:20000`
- [ ] 防火墙没有阻止连接

### 应用状态
- [ ] 两个应用都在运行
- [ ] 没有端口冲突
- [ ] 应用已重启

## 如果仍然不工作

### 临时解决方案 1: 使用会话超时

在 application.yml 中设置较短的会话超时：
```yaml
spring:
  session:
    timeout: 2m  # 2分钟后自动过期
```

### 临时解决方案 2: 前端轮询

在页面中添加 JavaScript 定期检查会话状态：
```javascript
setInterval(function() {
    fetch('/api/session/check')
        .then(response => response.json())
        .then(data => {
            if (!data.authenticated) {
                window.location.href = '/';
            }
        });
}, 10000); // 每10秒检查一次
```

### 联系支持

如果以上步骤都无法解决，请提供：
1. Keycloak 客户端配置截图
2. App A 和 App B 的完整启动日志
3. 单点登出测试时的完整控制台输出
4. Keycloak Events 页面的 LOGOUT 事件详情

## 成功标志

当单点登出正常工作时，你会看到：

1. **App B 控制台**（当 App A 退出时）：
```
========================================
【App B】收到 Backchannel logout 请求
时间: 2025-01-31T14:30:15
来源 IP: 127.0.0.1
Logout token: 存在
会话已成功失效
========================================
```

2. **用户体验**：
- 在 App A 退出登录
- 刷新 App B 页面
- 立即显示登录页面，需要重新输入凭证

这就是真正的单点登出！
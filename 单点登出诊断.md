# 单点登出诊断指南

## 问题现象

App B 退出登录后，App A 并没有退出登录。

## 根本原因

**Keycloak 的 Backchannel logout 机制要求 Keycloak 服务器能够主动访问应用的 Backchannel logout URL。**

在你的环境中：
- Keycloak 服务器：`keycloak.fairy.vip`（云端/公网）
- App A：`appa.tbk.com`（本地，通过 hosts 文件映射到 127.0.0.1）
- App B：`appb.tbk.com`（本地，通过 hosts 文件映射到 127.0.0.1）

**问题**：Keycloak 服务器无法访问你本地的 `appa.tbk.com` 和 `appb.tbk.com`，因此无法发送 Backchannel logout 请求。

## 诊断步骤

### 1. 检查应用日志

在 App B 退出登录后，查看 App A 的控制台输出：

**如果看到**：
```
========================================
【App A】收到 Backchannel logout 请求
时间: 2025-01-31T...
...
========================================
```
说明 Backchannel logout 正常工作 ✅

**如果没有看到**：
说明 Keycloak 没有发送 Backchannel logout 请求到 App A ❌

### 2. 测试端点可访问性

运行测试脚本：
```bash
test-backchannel-logout.bat
```

**预期结果**：
```
Logout successful
```

这只能证明端点在本地可访问，但不能证明 Keycloak 服务器可以访问。

### 3. 从 Keycloak 服务器测试（如果有权限）

如果你能访问 Keycloak 服务器，尝试：
```bash
curl -X POST http://appa.tbk.com/logout/connect/back-channel/keycloak
```

**如果失败**：说明 Keycloak 无法访问你的本地应用 ❌

## 解决方案

### 方案 1：使用同一个 client-id（推荐用于本地开发）

**优点**：
- 配置简单
- 无需 Keycloak 访问本地应用
- 适合本地开发和测试

**缺点**：
- 不是真正的自动单点登出
- 需要用户手动刷新页面

**实施步骤**：

1. 修改 `app-a/src/main/resources/application.yml`：
```yaml
spring:
  security:
    oauth2:
      client:
        registration:
          keycloak:
            client-id: management-api
```

2. 修改 `app-b/src/main/resources/application.yml`：
```yaml
spring:
  security:
    oauth2:
      client:
        registration:
          keycloak:
            client-id: management-api
```

3. 重启应用

4. 在页面上添加提示："退出登录后，请刷新其他应用页面"

### 方案 2：使用内网穿透工具（推荐用于演示）

使用工具如 ngrok、frp 等，将本地应用暴露到公网。

**使用 ngrok 示例**：

1. 安装 ngrok: https://ngrok.com/download

2. 启动 App A 和 App B

3. 为 App A 创建隧道：
```bash
ngrok http 10000
```
会得到一个公网 URL，如：`https://abc123.ngrok.io`

4. 为 App B 创建隧道（需要另一个终端）：
```bash
ngrok http 20000
```
会得到另一个公网 URL，如：`https://def456.ngrok.io`

5. 在 Keycloak 中更新 Backchannel logout URL：
   - app-a: `https://abc123.ngrok.io/logout/connect/back-channel/keycloak`
   - app-b: `https://def456.ngrok.io/logout/connect/back-channel/keycloak`

6. 更新应用配置中的 redirect URIs

**优点**：
- 真正的自动单点登出
- 可以演示给他人看

**缺点**：
- 需要额外的工具
- 免费版 ngrok 每次启动 URL 都会变化
- 不适合生产环境

### 方案 3：部署到公网服务器（推荐用于生产环境）

将应用部署到公网服务器，使用真实的域名。

**优点**：
- 真正的自动单点登出
- 生产环境标准方案
- 稳定可靠

**缺点**：
- 需要服务器和域名
- 配置相对复杂

### 方案 4：使用前端轮询（折中方案）

保持独立的 client-id，但在前端添加轮询检查会话状态。

**优点**：
- 无需 Keycloak 访问本地应用
- 准自动单点登出（5-10秒延迟）

**缺点**：
- 有延迟
- 增加网络请求

**实施步骤**：

1. 在 HomeController 中添加会话检查端点（已实现）

2. 在 home.html 中添加轮询脚本：
```javascript
setInterval(function() {
    fetch('/api/session/check')
        .then(response => response.json())
        .then(data => {
            if (!data.authenticated) {
                window.location.href = '/';
            }
        });
}, 5000); // 每5秒检查一次
```

## 推荐方案对比

| 方案 | 本地开发 | 演示 | 生产环境 | 实现难度 | 用户体验 |
|------|---------|------|---------|---------|---------|
| 方案1: 同一个client-id | ✅ 推荐 | ⚠️ 可用 | ❌ 不推荐 | ⭐ 简单 | ⭐⭐ 需手动刷新 |
| 方案2: 内网穿透 | ⚠️ 可用 | ✅ 推荐 | ❌ 不推荐 | ⭐⭐ 中等 | ⭐⭐⭐⭐ 自动登出 |
| 方案3: 公网部署 | ❌ 不适用 | ✅ 推荐 | ✅ 推荐 | ⭐⭐⭐ 复杂 | ⭐⭐⭐⭐⭐ 完美 |
| 方案4: 前端轮询 | ✅ 推荐 | ✅ 推荐 | ⚠️ 可用 | ⭐⭐ 中等 | ⭐⭐⭐ 有延迟 |

## 当前建议

**对于你的情况（本地开发 + 云端 Keycloak）**：

建议使用**方案 1：同一个 client-id**

理由：
1. 配置最简单
2. 无需额外工具
3. 适合本地开发和测试
4. 单点登录完全正常
5. 单点登出只需用户手动刷新（可接受）

如果需要演示真正的自动单点登出，可以临时使用**方案 2：内网穿透**。

## 验证 Backchannel logout 是否工作

### 测试步骤

1. 重启应用
2. 在 App A 登录
3. 访问 App B（自动登录）
4. 在 App B 退出登录
5. **立即查看 App A 的控制台输出**

### 预期结果

**如果 Backchannel logout 工作**：
```
========================================
【App A】收到 Backchannel logout 请求
时间: 2025-01-31T12:34:56
来源 IP: xxx.xxx.xxx.xxx
Logout token: 存在
会话已成功失效
========================================
```

**如果 Backchannel logout 不工作**：
控制台没有任何输出

## 总结

Backchannel logout 是一个服务器到服务器的通信机制。如果 Keycloak 服务器无法访问你的应用服务器，这个机制就无法工作。

对于本地开发环境，使用同一个 client-id 是最实用的方案。

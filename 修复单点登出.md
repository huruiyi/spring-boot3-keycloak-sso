# 修复单点登出 - 内网环境

## 当前状态

- ✅ 使用独立的 client-id（app-a 和 app-b）
- ✅ 内网环境，Keycloak 可以访问应用
- ❌ App A 退出后，App B 没有退出

## 问题诊断

### 步骤 1: 检查应用日志

在 App A 退出登录后，立即查看 App B 的控制台输出。

**如果看到**：
```
========================================
【App B】收到 Backchannel logout 请求
时间: ...
会话已成功失效
========================================
```
说明 Keycloak 已经发送了请求，但会话管理有问题。

**如果没有看到**：
说明 Keycloak 没有发送 Backchannel logout 请求，需要检查 Keycloak 配置。

### 步骤 2: 验证 Keycloak 配置

在 Keycloak Admin Console 中检查：

#### app-a 客户端配置

1. 进入 `Clients` → `app-a` → `Advanced` 标签
2. 找到 **Logout settings** 部分
3. 确认以下配置：

**Backchannel logout URL**:
```
http://appa.tbk.com/logout/connect/back-channel/keycloak
```

**Backchannel logout session required**: `ON` ✅

**Backchannel logout revoke offline sessions**: `ON` ✅

#### app-b 客户端配置

1. 进入 `Clients` → `app-b` → `Advanced` 标签
2. 找到 **Logout settings** 部分
3. 确认以下配置：

**Backchannel logout URL**:
```
http://appb.tbk.com/logout/connect/back-channel/keycloak
```

**Backchannel logout session required**: `ON` ✅

**Backchannel logout revoke offline sessions**: `ON` ✅

### 步骤 3: 测试端点可访问性

从 Keycloak 服务器（或同一内网的任何机器）测试：

```bash
curl -X POST http://appa.tbk.com/logout/connect/back-channel/keycloak -d "logout_token=test"
curl -X POST http://appb.tbk.com/logout/connect/back-channel/keycloak -d "logout_token=test"
```

**预期结果**：
```
Logout successful
```

如果失败，说明 Keycloak 无法访问应用的 Backchannel logout URL。

## 常见问题和解决方案

### 问题 1: Keycloak 无法解析域名

**症状**: Keycloak 日志中显示无法连接到 Backchannel logout URL

**原因**: Keycloak 服务器无法解析 `appa.tbk.com` 和 `appb.tbk.com`

**解决方案**:

#### 方案 A: 在 Keycloak 服务器的 hosts 文件中添加映射

如果 Keycloak 运行在 Linux 服务器上：
```bash
sudo vi /etc/hosts
```

添加：
```
<App-A-IP>  appa.tbk.com
<App-B-IP>  appb.tbk.com
```

如果 Keycloak 运行在 Windows 服务器上：
```
C:\Windows\System32\drivers\etc\hosts
```

添加：
```
<App-A-IP>  appa.tbk.com
<App-B-IP>  appb.tbk.com
```

#### 方案 B: 使用 IP 地址

在 Keycloak 中配置 Backchannel logout URL 时使用 IP 地址：

**app-a**:
```
http://<App-A-IP>:10000/logout/connect/back-channel/keycloak
```

**app-b**:
```
http://<App-B-IP>:20000/logout/connect/back-channel/keycloak
```

### 问题 2: 会话管理问题

**症状**: 收到了 Backchannel logout 请求，但会话没有失效

**原因**: Spring Security 的会话管理配置不正确

**解决方案**: 已在代码中实现，确保应用已重启

### 问题 3: CSRF 保护阻止请求

**症状**: Backchannel logout 请求返回 403 Forbidden

**原因**: CSRF 保护阻止了 POST 请求

**解决方案**: 已在 SecurityConfig 中配置 CSRF 例外：
```java
.csrf(csrf -> csrf
    .csrfTokenRepository(new HttpSessionCsrfTokenRepository())
    .ignoringRequestMatchers("/logout", "/logout/connect/back-channel/keycloak")
)
```

### 问题 4: 会话 Cookie 作用域问题

**症状**: 会话失效了，但浏览器仍然显示已登录

**原因**: 浏览器缓存了页面或 Cookie 作用域不正确

**解决方案**: 
1. 刷新页面（Ctrl + F5 强制刷新）
2. 检查 Cookie 配置（已在 application.yml 中配置）

## 完整的测试流程

### 1. 准备工作

1. 确认 Keycloak 中已正确配置 app-a 和 app-b 客户端
2. 确认 Backchannel logout URL 已配置
3. 确认 Keycloak 可以访问应用的 URL

### 2. 重启应用

```bash
restart-all.bat
```

### 3. 清除浏览器数据

- 按 `Ctrl + Shift + Delete`
- 清除所有 Cookie 和缓存

### 4. 测试单点登录

1. 访问 http://appa.tbk.com/
2. 点击登录，输入凭证
3. 登录成功后，访问 http://appb.tbk.com/
4. 应该自动登录，无需输入密码 ✅

### 5. 测试单点登出

1. 在 App A 点击"退出登录"
2. **立即查看 App B 的控制台输出**
3. 应该看到：
   ```
   ========================================
   【App B】收到 Backchannel logout 请求
   ...
   ========================================
   ```
4. 刷新 App B 页面（或等待几秒）
5. 应该显示登录页面 ✅

### 6. 反向测试

1. 重新登录到两个应用
2. 在 App B 点击"退出登录"
3. **立即查看 App A 的控制台输出**
4. 应该看到 Backchannel logout 请求
5. 刷新 App A 页面
6. 应该显示登录页面 ✅

## 调试技巧

### 1. 启用详细日志

在 application.yml 中已配置：
```yaml
logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.security.oauth2: DEBUG
```

### 2. 查看 Keycloak 事件日志

1. 登录 Keycloak Admin Console
2. 进入 `Realm Settings` → `Events` 标签
3. 启用 `Save Events`
4. 查看 `Login Events` 和 `Admin Events`
5. 查找 LOGOUT 事件

### 3. 使用网络抓包工具

使用 Wireshark 或 Fiddler 抓取 Keycloak 到应用的 HTTP 请求，查看：
- Keycloak 是否发送了 Backchannel logout 请求
- 请求的内容是什么
- 应用返回了什么响应

### 4. 测试端点

运行测试脚本：
```bash
test-backchannel-logout.bat
```

或手动测试：
```bash
curl -v -X POST http://appa.tbk.com/logout/connect/back-channel/keycloak -d "logout_token=test"
```

查看详细的 HTTP 响应。

## 如果仍然不工作

### 临时解决方案：使用较短的会话超时

在 application.yml 中设置：
```yaml
spring:
  session:
    timeout: 5m  # 5分钟后自动过期
```

这样即使 Backchannel logout 不工作，会话也会在 5 分钟后自动过期。

### 联系支持

如果以上所有步骤都无法解决问题，请提供：
1. App A 和 App B 的完整控制台日志
2. Keycloak 的事件日志
3. Keycloak 客户端配置截图
4. 网络连通性测试结果

## 预期的工作流程

```
用户在 App A 点击退出
    ↓
App A 调用 /logout
    ↓
Spring Security 触发 OidcClientInitiatedLogoutSuccessHandler
    ↓
重定向到 Keycloak logout endpoint
    ↓
Keycloak 清除全局会话
    ↓
Keycloak 查找该用户的所有活动客户端（app-a 和 app-b）
    ↓
Keycloak 向 app-a 发送 POST 请求
    POST http://appa.tbk.com/logout/connect/back-channel/keycloak
    ↓
Keycloak 向 app-b 发送 POST 请求
    POST http://appb.tbk.com/logout/connect/back-channel/keycloak
    ↓
App A 和 App B 的 BackchannelLogoutController 收到请求
    ↓
使各自的会话失效
    ↓
Keycloak 重定向用户到 post_logout_redirect_uri
    ↓
用户访问任何应用都需要重新登录
```

## 成功标志

当单点登出正常工作时，你应该看到：

1. **App A 控制台**（在 App B 退出时）：
   ```
   ========================================
   【App A】收到 Backchannel logout 请求
   时间: 2025-01-31T...
   来源 IP: <Keycloak-IP>
   Logout token: 存在
   会话已成功失效
   ========================================
   ```

2. **App B 控制台**（在 App A 退出时）：
   ```
   ========================================
   【App B】收到 Backchannel logout 请求
   时间: 2025-01-31T...
   来源 IP: <Keycloak-IP>
   Logout token: 存在
   会话已成功失效
   ========================================
   ```

3. **用户体验**：
   - 在任一应用退出后
   - 刷新另一个应用的页面
   - 立即显示登录页面

# 单点登出说明

## 当前实现

使用同一个 `client-id: management-api`，实现了**准单点登出**。

### 工作方式

1. **单点登录（SSO）**：✅ 完全支持
   - 在 App A 登录后，访问 App B 无需重新登录
   - Keycloak 自动处理会话共享

2. **单点登出（SLO）**：⚠️ 部分支持
   - 在 App A 登出后，Keycloak 全局会话被清除
   - App B 的本地会话仍然有效
   - **用户需要刷新 App B 页面**才会被要求重新登录

### 用户体验

- 在任一应用登出后，**请刷新其他应用页面**以完成登出
- 或者等待会话超时（30分钟）

## 如何实现真正的单点登出

如果需要自动单点登出（App A 登出后，App B 立即失效），需要：

### 步骤 1：在 Keycloak 中创建独立客户端

1. 创建 `app-a` 客户端
2. 创建 `app-b` 客户端
3. 为每个客户端配置 Backchannel logout URL

### 步骤 2：更新应用配置

将 `client-id` 改为各自的客户端 ID：
- App A: `client-id: app-a`
- App B: `client-id: app-b`

### 步骤 3：配置 Backchannel Logout

在 Keycloak 中为每个客户端配置：
- **app-a**: Backchannel logout URL = `http://appa.tbk.com/logout/connect/back-channel/keycloak`
- **app-b**: Backchannel logout URL = `http://appb.tbk.com/logout/connect/back-channel/keycloak`

### 工作原理

```
用户在 App A 登出
    ↓
Keycloak 清除全局会话
    ↓
Keycloak 向所有客户端发送 Backchannel logout 请求
    ↓
App A 和 App B 都收到通知并清除本地会话
    ↓
用户访问任何应用都需要重新登录
```

## 推荐方案

### 对于快速上线项目
使用当前方案（同一个 client-id），在页面上添加提示即可。

### 对于生产环境
使用独立 client-id + Backchannel logout，实现真正的单点登出。

## 技术限制

使用同一个 `client-id` 时：
- ❌ Keycloak 的 Backchannel logout URL 只能配置一个
- ❌ 无法同时通知多个应用
- ❌ 应用的本地会话是独立的

使用独立 `client-id` 时：
- ✅ 每个客户端有独立的 Backchannel logout URL
- ✅ Keycloak 可以同时通知所有应用
- ✅ 实现真正的单点登出
